import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  searchContainer: {
    padding: 10,
    paddingTop: 8,
    paddingBottom: 8,
    shadowOpacity: 0.02,
    shadowRadius: 1.0,
    minHeight: 60,
  },
});
